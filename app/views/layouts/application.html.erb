<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Linktree" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js" integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" crossorigin="anonymous"></script>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <nav class="navbar bg-body-tertiary">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">
        <img src="https://images.icon-icons.com/3912/PNG/512/linktree_logo_icon_247832.png" alt="Logo" width="30" height="24" class="d-inline-block align-text-top">
        LinkTree
      </a>
      <div class="d-flex">
        <%=button_to "Login",new_session_path,class:"btn btn-outline-primary me-2", type:"button", method: :get unless authenticated?%>
        <%=button_to "Register",session_register_path,class:"btn btn-outline-primary me-2", type:"button", method: :get unless authenticated?%>
        <%=button_to "Logout",session_path,class:"btn btn-outline-primary me-2", type:"button", method: :delete if authenticated?%>
        <%= button_to "Create your tree", new_tree_path,class:"btn btn-outline-primary me-2", type:"button", method: :get if authenticated? and Current.user.tree.nil? %>
      </div>
    </div>
  </nav>
    <%= yield %>
  </body>
</html>
